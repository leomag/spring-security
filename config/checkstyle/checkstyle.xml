<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <!-- Проверка длины строки (увеличена для Spring аннотаций) -->
    <module name="LineLength">
        <property name="max" value="140"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://|@see|@link"/>
    </module>

    <!-- Исключаем файлы из типовых папок -->
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value="module\-info\.java$"/>
    </module>

    <module name="TreeWalker">
        <!-- Отключаем требование Javadoc на всех методах (в Spring это излишне) -->
        <module name="MissingJavadocMethod">
            <property name="severity" value="ignore"/>
        </module>
        <module name="MissingJavadocType">
            <property name="severity" value="ignore"/>
        </module>

        <!-- Именование -->
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="MethodName"/>
        <module name="PackageName"/>
        <module name="ParameterName"/>
        <module name="TypeName"/>

        <!-- Пробелы и форматирование -->
        <module name="EmptyBlock"/>
        <module name="NeedBraces"/>
        <module name="WhitespaceAround"/>
        <module name="WhitespaceAfter"/>
        <module name="NoWhitespaceBefore"/>

        <!-- Импорты -->
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>

        <!-- Потенциальные ошибки -->
        <module name="EqualsHashCode"/>
        <module name="HiddenField">
            <property name="ignoreSetter" value="true"/>
            <property name="ignoreConstructorParameter" value="true"/>
        </module>
        <module name="IllegalThrows"/>
        <module name="ModifiedControlVariable"/>
        <module name="StringLiteralEquality"/>

        <!-- Убираем проверку магических чисел (часто нужны в конфигурациях) -->
        <module name="MagicNumber">
            <property name="severity" value="ignore"/>
        </module>
    </module>
</module>